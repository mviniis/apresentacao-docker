apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: deployment-container-back
  template:
    metadata:
      labels:
        app: deployment-container-back
    spec:
      containers:
      - name: deployment-container-back
        image: imagem-container-back:lastest
        ports:
        - containerPort: 80
        resources:
          limits:
            cpu: "500m"
            memory: "128Mi"
          requests:
            cpu: "250m"
            memory: "64Mi"
