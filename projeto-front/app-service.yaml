apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: deployment-container-front
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
